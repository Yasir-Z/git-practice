# Understand the project 
  frontend: 
    which programming language used ?
    On which port it's running ?
 
  backend:
    which programming language used ?
    On which port it's running ?

  dependencies requirements:
    requirments.txt (if python app)
    package.json (if java app)

# Run the app locally
  run frontend with python (python3 -m frontendfile)  
  
  run back end with node (node backendfile)






